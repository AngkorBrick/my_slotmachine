<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    	<p>There is a conversation at project <b>*Lego NXT Slot Machine</b>.</p>
    	<blockquote><i>
    		<div style="font-weight: bold;">
	    		<p><%=@comment.created_at%>, <%=@comment.name%>(<%=@comment.email%>)</p>
	    		<p>"<%=@comment.comment%>"</p>
    		</div>
    		<div style="margin-left: 50px;">
			    <% unless @reply_list.nil? || @reply_list.empty? %>
			    	<%@reply_list.each_with_index do |r,index| %>
				    		<% if(r.id==@reply.id)%>
					    		<div style="color: brown;font-weight: bold;">
						    		<p><%=r.created_at%>, <%=r.name%>(<%=r.email%>)</p>
				    				<p>"<%=r.reply%>"</p>
			    				</div>
			    			<% else %>
			    				<p><%=r.created_at%>, <%=r.name%>(<%=r.email%>)</p>
				    			<p>"<%=r.reply%>"</p>
			    			<%end%>
			    	<%end%>
			    <%end%>	
		    </div>	
    	</i></blockquote>
    <p>**Please do not reply to this email. To access the comment page, <%= link_to 'click here',@url%>.</p>
  </body>
</html>
